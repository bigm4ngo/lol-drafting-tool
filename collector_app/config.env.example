# Create config.env beside this file. Never commit or share it.
# This is the Ubuntu data collector's Riot API key. It is required for the
# collector to talk to Riot. Development keys expire (~24h); keep it fresh via
# the Riot Developer Portal, or replace automatically by your own tooling.
RIOT_API_KEY=RGAPI-replace-with-a-fresh-development-key
