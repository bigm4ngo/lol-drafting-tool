# Create config.env beside this file. Never commit or share it.
# On the PC this key is only needed to manually scrape Riot (optional; the
# server is the default collector). The collector on Ubuntu uses its own copy.
RIOT_API_KEY=RGAPI-replace-with-a-fresh-development-key
